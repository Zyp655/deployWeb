import LoginClient from './LoginClient';

export const metadata = {
  title: 'Đăng nhập - Hoang Food',
  description: 'Đăng nhập Hoang Food',
};

export default function Login() {
  return <LoginClient />;
}
