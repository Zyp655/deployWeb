import RegisterClient from './RegisterClient';

export const metadata = {
  title: 'Đăng ký - Hoang Food',
  description: 'Tạo tài khoản Hoang Food',
};

export default function Register() {
  return <RegisterClient />;
}
